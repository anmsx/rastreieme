<div class="container_12 clearfix">
	<section class="portlet grid_12"> 
	    <header>
	        <h2>Cadastre-se</h2> 
	    </header>
	    <section>
			<h3>Gostou ? Está quase lá!</h3>
			<h5>Algumas informações e você tem acesso completo a tudo que conheceu!</h5>

			<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :class => 'form has-validation') do |f| %>

				<% if devise_error_messages!.size > 0 -%>
					<div>
						<div class='message error closeable'>
							<span class="message-close"></span>
							<%= devise_error_messages! %>
						</div>
					</div>
				<% end -%>

				<div>
					<%= f.label :name, 'Nome', :class => 'form-label' %>
					<div class="form-input">
						<%= f.text_field :name %>
					</div>
					Seu nome, ou nome de sua empresa...
				</div>

				<br />

				<div>
					<%= f.label :email, :class => 'form-label' %>
					<div class="form-input">
						<%= f.email_field :email %>
					</div>
					Não se preocupe: Utilizaremos apenas para envio de informações importantes.<br />
					O seu email será também utilizado para login.
				</div>

				<br />
							
				<div>
					<%= f.label :password, 'Senha', :class => 'form-label' %>
					<div class="form-input">
						<%= f.password_field :password %>
					</div>
					Escolha uma senha com pelo menos 5 caracteres.
				</div>

				<br />

				<div>
					<%= f.label :password_confirmation, 'Confirme a sua senha', :class => 'form-label' %>
					<div class="form-input">
						<%= f.password_field :password_confirmation %>
					</div>
					Confirme a senha digitada acima.
				</div>

				<br />

				<div>
					<div class="form-input">
						<%= f.submit "Enviar" %>
					</div>
				</div>
			<% end %>

			<br />

			<div><%= render "links" %></div>

		</section>
	</section>
</div>
<div class="clear"></div>